# Override for OpenMRS with PostgreSQL instead of MySQL - COMMENTED OUT FOR NOW
# Usage: docker compose -f docker-compose.yml -f docker-compose.openmrs-postgres.yml --profile openmrs-postgres up -d
#
# This will:
# - Start openmrs-db-postgres (PostgreSQL) alongside openmrs-db (MySQL stays for other services)
# - Configure openmrs-backend to use PostgreSQL
# - Superset will connect to OpenMRS PostgreSQL
#
# To re-enable: uncomment the openmrs-db-postgres service and openmrs-db-postgres-data volume in docker-compose.yml

# No overrides while PostgreSQL for OpenMRS is disabled (openmrs-db-postgres service commented in docker-compose.yml)
services: {}
